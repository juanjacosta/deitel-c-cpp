/*
    Indique el encabezado para cada una de las siguientes funciones

    a) La función hipotenusa que toma dos argumentos de punto 
       flotante de precisión doble, lado1 y lad2, y devuelve un 
       resultado de punto flotante de precisión doble.
    b) La funcion elMenor que toma tres enteros, x, y, z e devuelve
       un entero.
    c) La función instrucciones que no recibe argumentos y no devuelve
       valor alguno [Nota: por lo general, dichas funciones se utilizan 
       para desplegar instrucciones para el usuario]
    d) La función intAfloat que toma un argumento entero, numero, y 
       devuelve un resultado en punto flotante.
*/
#include <stdio.h>

double hipotenusa(double lado1, double lado2);
int elMenor(int x, int y, int z);
void instrucciones(void);
float intAfloat(int numero);

int main()
{


    return 0;
}

double hipotenusa(double lado1, double lado2)
{
    double h;

    return h; 
}

int elMenor(int x, int y, int z)
{
    int menor;

    return menor;   
}

void instrucciones(void)
{
    
}

float intAfloat(int numero)
{
    float num;

    return num; 
}